namespace CoursesManager.UI.Enums
{
    // These types correspond with the 3 options in the toastmessages.
    // Each type will show the corresponding color and icon in the message.
    public enum ToastType
    {
        None,
        Warning,
        Error,
        Confirmation
    }

}
